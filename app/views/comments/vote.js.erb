<% if @vote.valid? %>
  $('#comment_<%= @comment.id %>_votes').html('<%= @comment.total_votes %> votes');
<% else %>
  $('#comment_vote_error_<%= @comment.to_param %>').show().fadeOut(3000);
<% end %>
